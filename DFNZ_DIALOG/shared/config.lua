Config = {}

Config.CamSettings = {
    enable = true, --> enable or disable camera moving
    transition = 1800, --> how long should the camera move to the ped (in ms)
    fast = true, --> if true the cam delets without animation
}